const Header = (props) => {
    return(
        <div>
        <h1>{props.heading}</h1>
        <h3>{props.slogan}</h3>
        




        </div>
    );
}

const Exercise2 = () => {
     return (

        <Header heading=" welcome to the app " slogan= "stepping into the new world " /> 
     );

}

export default Exercise2;